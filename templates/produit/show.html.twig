{% extends 'base.html.twig' %}

{% block body %}
    <h1>
        {{ product.name }}
        <small>
            <div class="badge badge-info">
                {{ product.price }}€
            </div>
        </small>
    </h1>
    <img src="/img/{{ product.imageName }}" class="card-img-top"alt="Image de {{ product.name }}">
    <p>{{ product.description }}</p>
{% endblock %}